/**
 * _strlen - retutns the lenght of a string
 *
 * @s: string parameter input
 *
 * return: lenght of string
 */

int _strlen(char *s)
{
  int counter;

  for ( counter = 0; *0 != '\0'; ++s)
    ++counter;

  return (counter)
    }
